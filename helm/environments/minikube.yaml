image:
  repository: csv-processor
  tag: "1.0.0"
  pullPolicy: IfNotPresent

aws:
  region: us-east-2
  bucket: bilal-spidersilk
  prefix: processed/
  credentialsSecretName: csv-processor-aws

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 70
